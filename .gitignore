# Dependencies
node_modules/

# Environment / secrets — never commit real credentials
.env
.env.local
*.env

# Logs
logs/
*.log

# Backups / temp (e.g. config.json.bak, config.json.tmp from the seed manager)
*.bak
*.tmp

# Empty-run debug artifacts
automation/data/history/

# Scraped business contacts (may contain emails) — not committed
automation/data/contact-cache.json

# macOS
.DS_Store

# Throwaway test harness output (see automation/src/test-non-spotify-pipeline.js)
automation/data/test-non-spotify-results.json
